apiVersion: v1
kind: Service
metadata:
  name: clusterpulse-nodeport
spec:
  type: NodePort
  selector:
    app: clusterpulse
    component: web
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30050